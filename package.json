{
  "name": "3d-pocketbell",
  "version": "6.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && vercel --prod"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vercel": "^39.0.0"
  }
}
